@model IEnumerable<Inventario.ViewModels.VMInventario>
@*@model List<Inventario.ViewModels.VMInventario>*@
@*@model List<RubricaWeb.ViewModels.VM_Rubrica>
    @model IEnumerable<Inventario.ViewModels.VMInventario>*@

@{
    ViewBag.Title = "ListadoInventario";
    List<SelectListItem> itemsTipoArticulos = (List<SelectListItem>)ViewBag.itemsTipoArticulos;
    //Inventario.ViewModels.VMInventario items = ViewBag.itemsTipoArticulos;
}
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

<br />
<div class="row">
    <div class="col-sm-12">
        <div>
            <h3>Inventario</h3>
        </div>
    </div>
</div>
<br />
@*[ ]*@
@using (Html.BeginForm())
{
    <div class="row">
        <div class="col-lg-9">
            <div class="form-group">
                <label class="col-sm-6" control-label>Buscar por tipo de articulos</label>
                <div class="col-sm-6">
                    @*@Html.DropDownListFor(Id_tipo_articulo => items, new SelectList(items.Id_tipo_articulo, "Value", "Text"), "Seleccione un tipo", htmlAttributes: new { @class = "form-control", @id = "Id_tipo_articulo" })*@


                    @Html.DropDownList("Id_tipo_articulo", itemsTipoArticulos, "Seleccione un tipo", htmlAttributes: new { @class = "form-control", @onchange = "refresh()" })

                </div>
                <br />

            </div>


        </div>
        <div class="col-lg-9">
            <table class="table table-bordered table-striped bg-light">
                <thead>
                    <tr>
                        <th class="text-center">
                            Nombre
                        </th>
                        <th class="text-center">
                            Modelo
                        </th>
                        <th class="text-center">
                            Marca
                        </th>
                        <th class="text-center">
                            Tipo
                        </th>
                        <th class="text-center">
                            Cantidad total
                        </th>
                        @*<th class="text-center">
                            Accion
                        </th>*@


                    </tr>
                </thead>
                <tbody>
                    @foreach (var VMInventario in Model)
                    {
                        <tr class="text-center">
                            <td>
                                @VMInventario.Nombre_articulo
                            </td>
                            <td>
                                @VMInventario.Modelo_articulo
                            </td>
                            <td>
                                @VMInventario.Desc_marca_articulo
                            </td>
                            <td>
                                @VMInventario.Desc_tipo_articulo
                            </td>
                            <td>
                                @VMInventario.Stock
                            </td>
                            @*<td>
                                <div class="dropdown">
                                    <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
                                        Acciones
                                    </button>
                                    <div class="dropdown-menu">
                                        <a class="dropdown-item" href="@Url.Action("ObtenerArticulo", "Inventario", new { @id_articulo=VMInventario.Id_articulo})">Editar</a>
                                        <a class="dropdown-item" href="@Url.Action("ObtenerArtAsignado", "Inventario", new { @VMInventario=VMInventario.Id_articulo})">Asignar/Desasignar</a>
                                        <a class="dropdown-item" href="@Url.Action("AsignarUsr", "Inventario", new { @VMInventario=VMInventario.Id_articulo})">Historial</a>
                                    </div>
                                </div>
                            </td>*@
                        </tr>
                    }
                </tbody>

            </table>

        </div>
        <div class="col-lg-9">
            <a href="@Url.Action("Index", "Home")" class="btn btn-success">Inicio</a>
        </div>
    </div>

}
<script>

    function refresh() {
        var cboTipoArt = document.getElementById("Id_tipo_articulo");
        console.log(cboTipoArt);
        if (cboTipoArt.value === null || cboTipoArt.value === undefined )
        {
            cboTipoArt.value == 0;
            cboTipoArt.addEventListener("change", refresh);
            window.location.href = "@Url.Content("~/Inventario/ListadoInventario")";
            console.log("Entra if");
        }
        else {
            cboTipoArt.addEventListener("change", refresh);
            window.location.href = "@Url.Content("~/Inventario/ListadoInventarioFiltrado")" + "?Id_tipo_articulo=" + cboTipoArt.value;
        }
        console.log(cboTipoArt);
        console.log("fuera if");
    }
</script>

